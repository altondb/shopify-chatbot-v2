{
  "name": "shopify-ai-chatbot",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "openai": "^4.10.0",
    "@vercel/kv": "^1.0.1"
  },
  "engines": {
    "node": "18.x"
  }
}
